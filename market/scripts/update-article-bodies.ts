import { prisma } from "../src/lib/db";
import { articleImage } from "../src/lib/placeholders";

async function main() {
  const body = `## مقدمه: چرا آردوینو برای IoT؟
آردوینو به خاطر سادگی، جامعه بزرگ و اکوسیستم غنی سنسورها، یکی از بهترین نقاط شروع برای ورود به اینترنت اشیا است. در این راهنما بدون پیچیدگی‌های غیرضروری، مسیر ساخت یک پروژه واقعی را طی می‌کنیم.

## انتخاب سخت‌افزار مناسب
برای شروع، یک برد آردوینو Uno یا Nano به‌همراه ماژول وای‌فای کافی است. اگر می‌خواهید همه چیز روی یک برد باشد، ESP32 گزینه بهتری است چون وای‌فای و بلوتوث داخلی دارد.

![نمایی نزدیک از برد توسعه برای پروژه‌های اینترنت اشیا](https://images.unsplash.com/photo-1518770660439-4636190af475?w=1200)

## راه‌اندازی محیط توسعه
Arduino IDE را نصب کنید، برد را انتخاب کنید و یک اسکچ ساده چشمک‌زن را آپلود کنید تا اتصال پورت سریال و درایور را مطمئن شوید. سپس کتابخانه‌های WiFi و MQTT را اضافه کنید.

\`\`\`cpp
#include <WiFi.h>

const char* ssid = "YOUR_SSID";
const char* password = "YOUR_PASSWORD";

void setup() {
  Serial.begin(115200);
  WiFi.begin(ssid, password);
  while (WiFi.status() != WL_CONNECTED) {
    delay(500);
    Serial.print(".");
  }
  Serial.println("Connected");
}

void loop() {}
\`\`\`

## اتصال سنسور و ارسال داده
یک سنسور دما/رطوبت (مثل DHT22) را وصل کنید و داده‌ها را هر چند ثانیه یک‌بار به یک broker مثل MQTT بفرستید. این پایه تقریباً همه پروژه‌های خانه هوشمند است.

- انتخاب برد مناسب (Uno، Nano، ESP32)
- اتصال DHT22 و نمایش روی سریال
- ارسال داده با MQTT به داشبورد
- نکات امنیتی و تغذیه پایدار

:::note نکته مهم
اگر پروژه شما نیاز به تغذیه باتری دارد، از ابتدا مصرف انرژی را طراحی کنید؛ مد خواب عمیق (deep sleep) می‌تواند عمر باتری را چند برابر کند.
:::

## جمع‌بندی و قدم بعدی
با همین مسیر می‌توانید مانیتورینگ محیط، کنترل رله و داشبورد ساده بسازید. قدم بعدی اتصال امن (TLS)، مدیریت OTA و مقیاس‌پذیری برای چند دستگاه است.`;

  await prisma.article.update({
    where: { slug: "iot-arduino-guide" },
    data: {
      title: "آموزش جامع اینترنت اشیا (IoT) با بردهای آردوینو",
      excerpt:
        "از اتصال سنسور تا ارسال داده به داشبورد ابری، قدم‌به‌قدم با آردوینو و ESP مسیر ساخت اولین گره IoT را طی می‌کنیم.",
      body,
      category: "اینترنت اشیا",
      dateLabel: "۲۴ مهر ۱۴۰۳",
      image: articleImage("IoT + Arduino"),
      published: true,
    },
  });

  // Enrich other articles that only have plain paragraphs
  const extras = [
    {
      slug: "arduino-boards-comparison",
      body: `## مقدمه
انتخاب برد آموزشی باید با بودجه، فضای فیزیکی و تعداد پین‌های موردنیاز هم‌خوان باشد.

## مقایسه کوتاه
Uno برای شروع و مستندسازی عالی است. Nano فضای کمتری می‌گیرد و Mega برای پروژه‌های پین‌محور مناسب‌تر است.

:::note نکته مهم
اگر به وای‌فای داخلی نیاز دارید، از ابتدا سراغ خانواده ESP32 بروید تا بعداً مجبور به افزودن شیلد نشوید.
:::

## جمع‌بندی
مدل را بر اساس پروژه واقعی انتخاب کنید، نه فقط قیمت ویترین.`,
    },
    {
      slug: "smd-soldering-guide",
      body: `## ابزار لازم
نوک باریک، فلاکس مناسب، پنس ظریف و تمرین روی برد آزمایشی پایه کار است.

## دمای پیشنهادی
دمای حدود ۳۲۰ تا ۳۵۰ درجه برای اکثر قطعات منطقی مناسب است.

![نمایی نزدیک از فرآیند لحیم‌کاری دقیق](https://images.unsplash.com/photo-1581092160562-40aa08e78837?w=1200)

:::note نکته مهم
بدون فلاکس کافی، پل لحیم و اتصال سرد خیلی سریع ایجاد می‌شود.
:::

## جمع‌بندی
با تمرین منظم، کیفیت اتصالات SMD شما به سطح تولید نزدیک می‌شود.`,
    },
  ];

  for (const e of extras) {
    await prisma.article.updateMany({
      where: { slug: e.slug },
      data: { body: e.body },
    });
  }

  console.log("Articles updated.");
}

main()
  .catch((e) => {
    console.error(e);
    process.exit(1);
  })
  .finally(async () => {
    await prisma.$disconnect();
  });
